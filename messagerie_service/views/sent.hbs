<div class="chat-container">
    {{#if messages.length}}
        {{#each messages}}
        <div class="message-card">
            <div class="message-header">
                <div class="sender-info">
                    <div class="sender-avatar" style="background: linear-gradient(135deg, #48bb78, #38a169);">
                        {{#if destinataire.nom}}
                            {{destinataire.nom}}
                        {{else}}
                            U
                        {{/if}}
                    </div>
                    <div>
                        <div class="sender-name">{{destinataire.nom}} {{destinataire.prenom}}</div>
                        <div class="message-time">{{date}}</div>
                    </div>
                </div>
            </div>
            <div class="message-preview">
                {{contenu}}
            </div>
            <a href="/chat/{{../userId}}/{{destinataire.id}}" class="conversation-link">
                Voir la conversation →
            </a>
        </div>
        {{/each}}
    {{else}}
        <div class="empty-state">
            <div>📤</div>
            <h3>Aucun message envoyé</h3>
            <p>Vous n'avez envoyé aucun message pour le moment.</p>
        </div>
    {{/if}}
</div>